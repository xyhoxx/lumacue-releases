# ประวัติการอัปเดต LumaCue

หน้านี้รวบรวมฟีเจอร์ใหม่ การแก้บั๊ก และการเปลี่ยนแปลงของ LumaCue ตั้งแต่รุ่น `0.1.1` จนถึงรุ่นล่าสุด ส่วนรายละเอียดภาษาอังกฤษอยู่ที่ [CHANGELOG_EN.md](CHANGELOG_EN.md)

**ภาษา:** [ไทย](CHANGELOG.md) | [English](CHANGELOG_EN.md)

## 0.8.9 - Hotfix: Auto DJ ไม่หยิบเพลงเวอร์ชันซ้ำ

### แก้ไข
- Auto DJ จะตัดเพลงที่เป็นเพลงเดียวกันแต่เป็นคนละอัปโหลดออกก่อนเติมคิว เช่น `Live Session`, `Acoustic`, `Remix`, `Official Video`, `Official Audio` และเวอร์ชันใกล้เคียง
- ตรวจทั้งตอนหาเพลงจากเพลงที่กำลังเล่นหรืออยู่ในคิว และตรวจซ้ำอีกครั้งก่อนเพิ่มเข้าคิว เพื่อไม่ให้เพลงเวอร์ชันอื่นหลุดเข้ามา

## 0.8.8 - Hotfix: ตอบกลับบอทเพื่อเลือกเพลงได้

### แก้ไข
- แก้การเลือกเพลงหรือศิลปินจากการ Reply กลับข้อความของ `xyhoxx_bot` เมื่อ Twitch เติม `@xyhoxx_bot` ไว้หน้าข้อความให้อัตโนมัติ ตอนนี้ LumaCue จะตัดเฉพาะ mention นี้ออกก่อนค้นหา เช่น Reply ว่า `@xyhoxx_bot BTS` จะค้นหา `BTS` ตามปกติ
- รองรับทั้งข้อความแชตแบบปกติและข้อมูล mention จาก EventSub โดยไม่กระทบคำขอเพลงทั่วไปหรือการ mention คนอื่นในแชต

## 0.8.7 - Hotfix: ข้อความตอบกลับคำขอเพลงสั้นลง

### ปรับปรุง
- ลดข้อความที่บอทส่งกลับแชต Twitch ให้สั้นและอ่านง่ายขึ้น โดยไม่เปลี่ยนการค้นหา การเข้าคิว การแลกแต้ม หรือการคืนแต้ม
- กรณีเพลงซ้ำ กำลังเล่นอยู่ ถูกบล็อก หาไม่เจอ หรือส่งเพลย์ลิสต์ จะไม่พิมพ์รายละเอียดซ้ำยาว ๆ ในแชต
- ข้อความเลือกศิลปินยังรองรับการพิมพ์ชื่อศิลปินและคำสั่ง `!pick` แต่จัดรูปแบบให้กระชับขึ้น
- ชื่อเพลงหรือศิลปินที่ยาวมากในข้อความเข้าคิวจะถูกย่อด้วย `...` ส่วนข้อมูลเพลงเต็มยังอยู่ในระบบ

## 0.8.6 - Hotfix: เลือกเพลงจากแชต Twitch ได้แม่นยำขึ้น

### แก้ไข
- เวลาเจอเพลงชื่อซ้ำ คนดูเลือกศิลปินที่แสดงอยู่ พิมพ์ศิลปินอื่น ส่งคำค้นใหม่ หรือตอบด้วยลิงก์เพลงได้ โดย LumaCue จะไม่เอาคำตอบไปต่อท้ายชื่อเพลงเดิมผิด ๆ
- รับคำตอบแบบอิสระเฉพาะข้อความที่ Reply กลับมาหาคำถามเลือกเพลง หรือขึ้นต้นด้วย `!pick` แชตทั่วไปจึงไม่ถูกส่งไปค้นหาเพลง
- ผูกคำตอบไว้กับคำถามเลือกเพลงล่าสุด คำตอบหรือข้อความจากคำถามเก่าที่มาช้าจะไม่เลือกเพลงผิด
- ถ้าส่งลิงก์ YouTube หรือ Spotify playlist ผ่านการแลกแต้ม LumaCue จะบอกให้ส่งลิงก์เพลงเดี่ยวแทน
- ลิงก์ YouTube แบบ `watch?v=...&list=...` ยังใช้ได้ โดย LumaCue จะเปิดวิดีโอจาก `v=` และไม่เอา playlist เข้ามาด้วย

## 0.8.5 - Hotfix: แก้ชื่อเพลงแรกหลังเปิดโปรแกรม

### แก้ไข
- แก้ชื่อเพลงแรกที่กู้จากคิวแล้วถูกระบบภาษาเขียนทับเป็น `Waiting for a song...`
- แยกชื่อเพลงออกจากข้อความคงที่ของระบบภาษา ข้อความตอนรอเพลงจึงยังเปลี่ยนตามภาษาที่เลือกได้โดยไม่ทับชื่อเพลง

## 0.8.4 - Hotfix: เพลงแรก Overlay และขั้นตอนปล่อยเวอร์ชัน

### แก้ไข
- แก้เพลงแรกหลังเปิดโปรแกรมที่บางครั้งยังขึ้น `Waiting for a song...` ทั้งที่เริ่มเล่นแล้ว
- เปลี่ยนค่าเริ่มต้นให้ Overlay แสดงค้างไว้แทนการหุบเอง LumaCue จะย้ายค่าที่บันทึกไว้เดิมให้ครั้งเดียว และยังเปิด auto-tuck กลับได้
- ตัวเลือก Overlay Settings ที่ใช้ไม่ได้กับค่าปัจจุบันจะขึ้นเป็นปิดใช้งาน แทนการหายไปจากหน้า
- แก้มุมแหลมรอบเงา native launcher โดยเก็บเงาให้อยู่ในขอบหน้าต่าง
- ถ้า Twitch เจอเพลงชื่อซ้ำ คนดูพิมพ์ชื่อศิลปินได้ตรง ๆ และค้นหาศิลปินอื่นเพิ่มได้เมื่อรายการแรกไม่มีชื่อที่ต้องการ
- ให้ Python backend และ Player iframe ใช้พอร์ตที่ desktop เลือกไว้เหมือนกัน ลดปัญหาเปิดโปรแกรมไม่ได้เมื่อพอร์ตเดิมไม่ว่าง

### ปรับปรุง
- เพิ่ม release gate ใน CI/CD เพื่อรันเทส Python, renderer, launcher, Discord, updater และ native launcher ก่อน build ไฟล์ปล่อย
- เพิ่ม smoke test ที่เปิดแอปจาก `win-unpacked` จริง แล้วเช็ก backend, Player iframe, Library และการปิด process หลังเทส
- อัปเดต `electron-updater`, `electron-builder` และ dependency ที่เกี่ยวข้องเป็นรุ่นที่อุดช่องโหว่แล้ว ตอนปล่อยรุ่นนี้ production dependency audit ไม่พบช่องโหว่ที่รู้จัก

## 0.8.3 - Hotfix: ลดการกินเครื่องและแก้รอยต่อ Player

### ปรับปรุง
- ลบ particle layer ที่แทบมองไม่เห็น หยุด animation กับ polling ตอนหน้าต่างหรือหน้านั้นถูกซ่อน และส่งเฉพาะเวลาเล่นแทนสถานะคิวทั้งชุดทุกวินาที
- ใช้ URL artwork เดียวกันกับ backdrop ของ shell และ Player พร้อมรวมการขยับตอน sidebar ขยายไว้ใน animation frame เดียว ไม่สร้าง bitmap เต็มหน้าต่างซ้ำ

### แก้ไข
- ให้การหมุน artwork ของเพลง Local Music และ direct audio เริ่มและหยุดตามสถานะเล่นจริง
- แก้ความต่างของสีและความสว่างระหว่าง shell กับ Player iframe รวมถึงเส้นรอยต่อหนึ่งพิกเซลข้าง sidebar

## 0.8.2 - Hotfix: ทำ native launcher ให้เข้าธีม

### แก้ไข
- เปลี่ยน native launcher จากหน้าต่าง Windows เดิมให้ใช้หน้าตาของ LumaCue ทั้งมุมโค้ง โลโก้ สถานะ progress bar สีครีม และ footer
- เอา title bar สีขาวกับ progress bar สีเขียวของ Windows ที่หลุดจากธีมออก

## 0.8.1 - Hotfix: Auto DJ และการย้ายไป native launcher

### แก้ไข
- Auto DJ ใช้เพลงที่กำลังเล่นกับเพลงถัดไปในคิวเป็นหลัก เพื่อหาเพลงที่เกี่ยวข้องหรือศิลปินเดียวกัน แทนการเริ่มจากประวัติ
- ค่าเริ่มต้นจะไม่ดึงเพลงเก่าจากประวัติกลับเข้าคิว ส่วนคนที่ตั้งแหล่ง history เองยังใช้ค่าเดิมได้

### เปลี่ยนแปลง
- เริ่มย้าย launcher จาก Electron ไปเป็น native Windows launcher ขนาดเล็ก โดย launcher เดิมอัปเดตตัวเองไปตัวใหม่ผ่าน manifest เดิมได้
- หลัง native launcher รับงานแล้ว จะลบเฉพาะไฟล์ Electron bootstrap ที่ไม่ใช้ เช่น resources, locales และ Chromium runtime แต่เก็บแอปรุ่นปัจจุบัน shared runtime และข้อมูลของคนใช้ไว้
- การถอนการติดตั้ง การเช็ก SHA-512, patch/app-only/runtime update และ offline fallback ยังทำงานเหมือนเดิม

## 0.8.0 - เชื่อม Twitch ได้เสถียรขึ้นและเริ่มแยกภาษา

### เพิ่ม
- เพิ่มปุ่ม Reconnect ฝั่ง Broadcaster เพื่อเชื่อม Twitch ใหม่โดยไม่ล้าง reward, EventSub หรือบอทที่ตั้งไว้
- เพิ่มสิทธิ์ `user:read:chat` ให้ Broadcaster เพื่อรับคำตอบเลือกศิลปินจากคนดูเมื่อใช้ `xyhoxx_bot`
- เพิ่มสวิตช์เปิด/ปิดการแลก Channel Points โดยคง reward ที่เลือกและการตั้งค่ารับคำขอเพลงไว้

### เปลี่ยนแปลง
- จัดข้อความจาก Twitch แบบเดียวกับช่องค้นหาในโปรแกรม ทั้ง Unicode, อักขระซ่อน คำสั่งด้านหน้า และช่องว่างซ้ำ
- รุ่นนี้ใช้ภาษาอังกฤษเป็นหลัก ปุ่มภาษาไทยยังแสดงไว้แต่กดไม่ได้ ระหว่างรอปรับ UI ให้รองรับข้อความไทย

### แก้ไข
- แก้การเลือกเพลง `1/2/3` จาก Twitch ที่ส่งคำถามได้ แต่รับคำตอบไม่ได้ตอนใช้บอทที่ตั้งไว้
- แก้คำค้นจากการแลกแต้มให้ตีความเหมือนการค้นหาในโปรแกรม
- เอาหัวข้อ `Request Song reward` ที่ซ้ำออกจาก Channel Points card
- เอา Album Motion ออกจาก Player และ OBS overlay เพราะผลที่ได้ยังไม่เข้ากับ LumaCue

## 0.7.9 - โหลด Spotify playlist เข้ามาได้

- เพิ่ม Spotify playlist ในแท็บ Playlist เดิม
- อ่านชื่อเพลงกับศิลปินจาก Spotify embed แล้วใช้ข้อมูลนั้นหาเพลงที่เล่นได้จาก YouTube Music หรือ YouTube
- ช่อง Playlist รับได้ทั้งลิงก์ YouTube และ Spotify

## 0.7.8 - แก้ Album Motion บน Overlay

- ทำให้ Album Motion ใช้กับ overlay แบบ compact และ pill ได้ดีขึ้น และลดปัญหาภาพเคลื่อนไหวขึ้นเป็นแผ่นสี่เหลี่ยมหลังการ์ด

## 0.7.7 - ปรับการตอบสนองของ UI และแก้ปัญหาความเสถียร

- เพิ่มเส้นใต้แท็บ Library ที่เคลื่อนต่อเนื่อง ปรับสวิตช์ให้ใช้รูปแบบเดียวกัน และแก้ Discord RPC ที่อัปเดตไม่ทันเมื่อเปลี่ยนเพลงเร็ว

## 0.7.6 - ทำหน้า Player ให้ต่อกับ shell เนียนขึ้น

- ทำให้ backdrop ของ artwork เปลี่ยนหน้าได้เนียนขึ้น และเอาช่องว่างด้านขวาของแท็บ Library ออก

## 0.7.5 - ใช้ CI/CD ปล่อยเวอร์ชันและแก้ Auto DJ

- เปลี่ยนมาใช้ CI/CD เป็นทางหลักสำหรับปล่อยเวอร์ชัน แก้ Auto DJ ให้เริ่มหาเพลงได้ตอนคิวว่าง และปรับไอคอน installer

## 0.7.4 - ให้ไอคอน Windows เปลี่ยนตามอัปเดต

- ไอคอนบน Desktop และ Start Menu เปลี่ยนตามรุ่นใหม่ได้โดยไม่ต้องติดตั้ง LumaCue ซ้ำ

## 0.7.3 - เปลี่ยนโลโก้และแยกข้อมูล Local Music ออกจากซอร์สโค้ด

- ใช้โลโก้ LumaCue แบบวงกลมในหน้าหลัก launcher และ tray พร้อมย้ายข้อมูล Local Music ที่ใช้ทดสอบบนเครื่องพัฒนาออกจากซอร์สโค้ด

## 0.7.2 - ลบไฟล์อัปเดตเก่าได้แน่นอนขึ้น

- ถ้า Windows ล็อกไฟล์ชั่วคราว updater จะลองลบเวอร์ชันเก่าและไฟล์ดาวน์โหลดอีกครั้ง แทนการทิ้งไฟล์ค้างไว้

## 0.7.1 - Auto DJ ไม่หยิบเพลงเดิมกลับมาเร็วเกินไป

- Auto DJ เลี่ยงเพลงที่เพิ่งเล่น และ launcher ลบไฟล์เวอร์ชันเก่าได้แน่นอนขึ้น

## 0.7.0 - Blocklist, Auto DJ และบอท Twitch

- เพิ่ม Global Blocklist ปรับการทำงานของ Auto DJ และ Local Music กำหนดให้ระบบแชตใช้ `xyhoxx_bot` ที่ตั้งไว้ และปรับหน้าตาของ desktop app ให้เป็นชุดเดียวกันมากขึ้น

## 0.6.8 - ซ่อน OAuth ticket หลังเชื่อม Twitch

- หลังเชื่อม Twitch สำเร็จ URL ในเบราว์เซอร์จะถูกล้าง ไม่ทิ้ง OAuth ticket ไว้ในแถบที่อยู่

## 0.6.7 - Hotfix: Twitch OAuth error 1010

- แก้ callback ของ Cloudflare Worker ที่ถูก Twitch ปฏิเสธในบางกรณี

## 0.6.6 - เชื่อม Twitch ผ่าน OAuth Broker

- เพิ่ม Cloudflare Worker สำหรับเชื่อม Twitch โดยไม่ใส่ client secret ไว้ในตัวโปรแกรม

## 0.6.5 - บีบอัด Local Music และแก้สถานะ Twitch

- บีบอัดไฟล์ Local Music เมื่อเครื่องทำได้ และแก้ข้อความตอน Twitch session หมดอายุให้ตรงกับสถานะจริง

## 0.6.4 - ไม่ปล่อยสถานะ Discord ค้าง

- ล้างสถานะ LumaCue ใน Discord เมื่อเปิดโปรแกรมโดยไม่มีเพลง หรือเมื่อปิดโปรแกรม

## 0.6.3 - ล้าง ZIP และ staging ที่ไม่ใช้แล้ว

- updater ลบ ZIP กับไฟล์ staging ที่ไม่ใช้แล้ว แต่ไม่แตะข้อมูลของคนใช้หรือ runtime ที่กำลังทำงาน

## 0.6.2 - เชื่อม Discord ผ่าน IPC โดยตรง

- เชื่อม Discord RPC ผ่าน IPC โดยตรง ลดการพึ่งไลบรารีภายนอกที่เคยทำให้โปรแกรมพัง

## 0.6.1 - Hotfix: Discord RPC ทำโปรแกรมพัง

- แก้ error ที่ทำให้ main process พังเมื่อ Discord IPC ถูกตัดระหว่างอัปเดตสถานะ

## 0.6.0 - เพิ่ม Local Music และ Auto DJ

- นำเข้าเพลงจากเครื่อง เล่นผ่าน Player เดิม และให้ Auto DJ เติมคิวจากข้อมูลการฟังได้

## 0.5.0 - ยกเครื่องหน้า Player

- ปรับ Player, artwork backdrop, queue panel, overlay และสถานะ Discord ใหม่ ให้ข้อมูลเพลงอ่านง่ายและใช้งานเป็นชุดเดียวกัน

## 0.4.0 - จัดระเบียบหน้า Desktop ใหม่

- วาง design system และโครงหน้า desktop ใหม่ ทั้ง shell, navigation, Twitch setup และ dialog ที่เข้ากับธีม

## 0.3.5 - Hotfix: backend เปิดไม่ขึ้น

- ใส่ `history.py` กลับเข้าแพ็กเกจ แก้ backend หยุดทำงานในรุ่น 0.3.2 ถึง 0.3.4

## 0.3.4 - ลบโค้ด Resolver ที่ไม่ใช้แล้ว

- ลบพารามิเตอร์และเอกสารเก่าที่ไม่ใช้งานออกจากตัวค้นหาเพลง

## 0.3.3 - เก็บกวาด Resolver และระบบคืนแต้ม

- รวมกติกาให้คะแนนเพลง ลบ dead code และแก้การคืนแต้ม Channel Points ตอนต้องเลือกศิลปิน

## 0.3.2 - เพิ่มสถิติการเล่น

- เพิ่มข้อมูลวิเคราะห์การเล่นเพลง และแก้การรวมโมดูล backend ในแพ็กเกจ

## 0.3.1 - คืนแต้มเมื่อเพลงไม่ได้เล่น

- คืน Channel Points ถ้าเพลงถูกข้าม ถูกลบ หรือเล่นไม่ได้ แทนการหักแต้มถาวรตั้งแต่เพลงเข้าคิว

## 0.3.0 - เปิดเพลงจากลิงก์ Spotify

- เพิ่มการรับลิงก์ Spotify track และแปลงเป็นคำค้นเพลงที่เล่นได้

## 0.2.28 - จำ learned rules ได้ต่อเนื่อง

- ให้กติกาที่จำจากการเลือกเพลงอ้างอิงคำขอเดิมได้ถูกต้อง และลด toast Twitch ที่ขึ้นซ้ำ

## 0.2.27 - ซ่อน Device Login หลังเชื่อมสำเร็จ

- ซ่อน panel device code เมื่อเชื่อม Twitch สำเร็จ

## 0.2.26 - เข้ารหัส Twitch token ในเครื่อง

- เก็บ access token กับ refresh token ด้วย Windows DPAPI และย้าย token เดิมให้อัตโนมัติ

## 0.2.25 - จำค่า Twitch และแสดงวันหมดอายุ token

- แสดงสถานะบัญชีตามวันหมดอายุ token และจำ reward กับข้อความทดสอบไว้

## 0.2.24 - แก้ Player ตอนเปิดแยกหน้าต่าง

- แก้ artwork กับปุ่มควบคุมที่ทับกันตอนเปิด Player นอก desktop shell

## 0.2.23 - ถามเพิ่มเมื่อเพลงไทยชื่อซ้ำ

- ถ้าเพลงไทยชื่อเดียวกันมีหลายศิลปิน LumaCue จะให้คนดูเลือกก่อนเข้าคิว

## 0.2.22 - รีเฟรช Twitch token อัตโนมัติ

- รีเฟรช token ระหว่างใช้งาน และลองคำขอเดิมอีกครั้งเมื่อ Twitch ตอบ 401

## 0.2.21 - learned rules เข้าใจชื่อไทยหลายแบบ

- ให้ learned rules จับคู่ชื่อไทยแบบย่อกับแบบเต็มได้มากขึ้น

## 0.2.20 - ใช้ direct audio เมื่อ embed ไม่ได้

- ใช้ direct audio เมื่อวิดีโอ YouTube เล่นผ่าน embed ไม่ได้ หากเพลงยังเข้าถึงได้

## 0.2.19 - เพิ่มวิธีเล่นเสียงโดยตรง

- เพิ่มเส้นทางเล่นเสียงโดยตรงสำหรับวิดีโอที่มีข้อจำกัดกับ embed

## 0.2.18 - ไม่เลือก live หรือ concert ง่ายเกินไป

- ลดโอกาสหยิบ live, concert หรือเวอร์ชันอื่นที่ไม่ตรงกับเพลงตอนใช้ fallback

## 0.2.17 - เช็กว่า embed เล่นได้ก่อนเข้าคิว

- เช็กว่าเพลงเล่นผ่าน embed ได้ก่อนรับเข้าคิว และบอกสาเหตุให้ชัดขึ้นถ้าเล่นไม่ได้

## 0.2.16 - หาเพลงสำรองเมื่อ embed error

- ถ้า YouTube embed ตัวแรกมีปัญหา LumaCue จะหาผลลัพธ์อื่นมาลองต่อ

## 0.2.15 - เลือกเพลงด้วย 1/2/3

- เพิ่มการตอบ `1/2/3` เพื่อเลือกศิลปินหรือเพลงที่ชื่อซ้ำ

## 0.2.14 - ใช้ reward เดิมหรือสร้างจาก LumaCue

- ให้เลือก reward ที่มีอยู่ หรือสร้าง/อัปเดต reward จาก LumaCue

## 0.2.13 - จำเพลงที่เลือกจากลิงก์ YouTube ได้

- แก้ให้ Resolver จดจำเพลงที่ผู้ใช้เลือกผ่าน YouTube URL ได้ถูกต้อง

## 0.2.12 - กัน resolver เลือกเพลงผิด

- เพิ่มเงื่อนไขกันผลค้นหาที่ไม่ตรงคำขอ และแก้ตัวกรองหลายจุด

## 0.2.11 - บอกจำนวนผลลัพธ์ใน Quick Add

- แสดงจำนวนผลลัพธ์และจัดการสถานะ Quick Add ให้ชัดเจนขึ้น

## 0.2.10 - เรียงผลลัพธ์ให้ตรงกับคำค้นมากขึ้น

- ปรับคะแนนและลำดับเพลงในรายการแนะนำ เพื่อให้ผลลัพธ์ที่ใกล้กับคำค้นอยู่ก่อน

## 0.2.9 - ใช้ชื่อศิลปินช่วยแยกเพลงชื่อซ้ำ

- ใช้ชื่อศิลปินที่พิมพ์มาช่วยกันการเลือกเพลงชื่อซ้ำผิดตัว

## 0.2.8 - ค้น YouTube เพิ่มเมื่อ YT Music ไม่เจอ

- ถ้า YouTube Music ไม่เจอเพลง จะค้น YouTube videos ต่อให้อัตโนมัติ

## 0.2.7 - อัปเดต launcher และแก้ EventSub

- เพิ่มการอัปเดต launcher และทำให้ EventSub เสถียรขึ้น

## 0.2.6 - กัน EventSub reconnect วน

- จัดการ token ที่หมดอายุก่อนเชื่อม EventSub ใหม่ ลดการ reconnect ซ้ำไม่จบ

## 0.2.5 - แยกคำไทยแล้วค้นใหม่

- แยกคำไทยเพื่อค้นหาใหม่เมื่อผลลัพธ์แบบติดกันไม่พบเพลง

## 0.2.4 - เลือกเพลงที่ต้องการด้วย YouTube URL

- ให้ผู้ใช้กำหนดผลลัพธ์ที่ต้องการผ่านลิงก์ YouTube

## 0.2.3 - ปรับการลากคิวและเกณฑ์เลือกเพลง

- ปรับการลากเรียงคิวให้ลื่นขึ้น และปรับเกณฑ์ที่ Resolver ใช้เลือกเพลง

## 0.2.2 - เปิดโปรแกรมเร็วขึ้นและแก้ IPC

- ลดเวลาเปิด desktop app และแก้การส่งข้อมูลระหว่าง shell กับ backend

## 0.2.1 - เพิ่ม motion ให้หน้า desktop และ Player

- เพิ่ม motion สำหรับ toast, dialog, navigation, queue, player และ OBS overlay

## 0.2.0 - ให้ resolver จำผลที่เลือก

- เพิ่มการจดจำการเลือกเพลง เช่น เลือกเสมอ, ไม่เลือก และเลือกเพลงนี้

## 0.1.42 - เช็กไฟล์อัปเดตใน build pipeline

- เพิ่มการตรวจ hash ของ launcher และจัดระเบียบคำสั่ง build/release

## 0.1.41 - แก้ถอนการติดตั้งและ launcher self-update

- แก้การถอนการติดตั้ง เพิ่ม self-update ของ launcher และเพิ่มการลองใหม่เมื่อแตกไฟล์ไม่สำเร็จ

## 0.1.40 - แยกชื่อ Online กับ Offline installer

- ตั้งชื่อไฟล์ Online กับ Offline setup ให้เลือกได้ง่ายขึ้น

## 0.1.39 - หน้าติดตั้งแบบ native

- เปลี่ยนไปใช้ setup stub แบบ native ให้ขั้นตอนติดตั้งอ่านง่ายขึ้น

## 0.1.38 - แก้การแตกไฟล์ ASAR

- แก้ปัญหาการแตกไฟล์ `.asar` ระหว่างติดตั้งและอัปเดต

## 0.1.37 - เพิ่ม Online installer

- เพิ่มตัวติดตั้งขนาดเล็กที่ลง launcher ก่อน แล้วให้ launcher ดาวน์โหลด LumaCue ต่อ

## 0.1.36 - ให้ launcher แสดงสถานะแทน installer

- ซ่อนการติดตั้งไว้เบื้องหลังและให้ launcher แสดงความคืบหน้าแทน

## 0.1.35 - ลดหน้าต่าง installer ที่เด้งขึ้นมา

- ทดลองลดหน้าต่าง installer ที่รบกวนผู้ใช้ระหว่างอัปเดต

## 0.1.34 - ซ่อนหน้าต่าง installer

- ปรับการซ่อนหน้าต่าง installer และแก้การเปิดโปรแกรมหลังติดตั้ง

## 0.1.33 - ใช้ launcher เป็นหน้าติดตั้งหลัก

- ให้ launcher เป็นหน้าหลักของขั้นตอนติดตั้งและอัปเดต

## 0.1.32 - ไม่คัดลอก runtime ซ้ำตอน patch

- เร่งการคัดลอกไฟล์ patch และไม่คัดลอก runtime เดิมซ้ำ

## 0.1.31 - ติดตั้งเบื้องหลังพร้อมหน้า launcher

- รวมการติดตั้งเงียบกับ UI แสดงสถานะของ launcher

## 0.1.30 - เปิดโปรแกรมได้เร็วขึ้นหลังอัปเดต

- ลดเวลารอก่อนเปิดโปรแกรมใหม่หลังใช้การอัปเดต

## 0.1.29 - ใช้หน้าต่างอัปเดตของ LumaCue

- เพิ่มหน้าต่างแจ้งการอัปเดตของ LumaCue แทน dialog ของระบบ

## 0.1.28 - ป้องกันการเปิดซ้ำและล้างสถานะชั่วคราวของ Launcher

- ป้องกันการเปิดหลายหน้าต่างพร้อมกัน และล้างสถานะชั่วคราวของ Launcher เมื่อไม่ต้องใช้งานแล้ว

## 0.1.27 - เพิ่ม patch update และแก้หน้า launcher

- เพิ่ม patch update และแก้ UI/การส่งต่อการเปิดโปรแกรมของ launcher

## 0.1.26 - ล้างไฟล์ดาวน์โหลดหลังอัปเดต

- ลบไฟล์ดาวน์โหลดและ manifest เก่าหลังอัปเดตโดยไม่ลบข้อมูลผู้ใช้

## 0.1.25 - ไม่ดาวน์โหลดซ้ำหลังติดตั้งใหม่

- ป้องกัน launcher ดาวน์โหลดซ้ำทันทีหลังติดตั้งเวอร์ชันเดียวกัน

## 0.1.24 - แตกไฟล์โดยไม่กิน RAM ทั้งก้อน

- อ่านและแตกไฟล์อัปเดตต่อเนื่องเพื่อลดการใช้หน่วยความจำ

## 0.1.23 - แตกไฟล์เร็วขึ้นและใช้ cache เดิม

- เร่งการแตกไฟล์และใช้ cache ที่ดาวน์โหลดแล้วเมื่อปลอดภัย

## 0.1.22 - ย้ายการแตกไฟล์มาใช้ JavaScript

- ย้ายการแตกไฟล์อัปเดตออกจาก PowerShell มาใช้ JavaScript ในโปรแกรม

## 0.1.21 - แก้ launcher ค้าง

- แก้ launcher ค้างหลังขั้นตอน apply update และเพิ่มการตรวจสถานะที่จำเป็น

## 0.1.20 - แก้สถานะ EventSub และ patch

- ทำให้สถานะ EventSub อัปเดตหลังเริ่ม listener และปรับความเสถียรของ patch

## 0.1.19 - แยก EventSub ของ Broadcaster กับ Bot

- แยก EventSub ของ Broadcaster และ Bot เป็น WebSocket คนละเส้นตามสิทธิ์ Twitch

## 0.1.18 - ใช้ตัวแตกไฟล์แบบ native

- ใช้เส้นทางแตกไฟล์แบบ native และแก้ compatibility ของ update package

## 0.1.17 - แสดงความคืบหน้าตอนแตกไฟล์

- แสดงความคืบหน้าขณะเตรียมไฟล์อัปเดตให้ชัดเจนขึ้น

## 0.1.16 - ลบ EventSub subscription เก่าก่อนต่อใหม่

- ลบ subscription เก่าก่อนสร้างใหม่เพื่อป้องกัน listener ซ้ำ

## 0.1.15 - กัน token Twitch จากคนละแอป

- เช็ก Client ID ของ token ที่เก็บไว้ ถ้าไม่ใช่ของ LumaCue จะให้เชื่อมบัญชีใหม่

## 0.1.14 - แยก app กับ runtime ใน manifest

- แยก app package ออกจาก runtime package เพื่อลดขนาดอัปเดต

## 0.1.13 - ให้ manifest ใหม่ใช้กับ launcher เก่าได้

- ให้ manifest ใหม่ยังทำงานกับ launcher รุ่นก่อนหน้าได้

## 0.1.12 - รวมขั้นตอนเปิดโปรแกรมหลังผ่าน Launcher

- รวมขั้นตอนส่งต่อจาก Launcher ไปยัง desktop app ให้ใช้เส้นทางเดียวกัน

## 0.1.11 - เริ่มใช้ patch update

- เริ่มใช้ patch update และรองรับ shared runtime ระหว่างเวอร์ชัน

## 0.1.10 - ปรับหน้า About / Updates

- ลบการ์ด runtime ที่ซ้ำซ้อน และเขียน release notes แบบ UTF-8 ไม่มี BOM

## 0.1.9 - แตก ZIP อัปเดตด้วย Node

- เปลี่ยนการแตก ZIP update เป็น native Node process และเพิ่มการตรวจ staged app

## 0.1.8 - แก้การส่งต่อจาก launcher

- แก้การส่งต่อจาก launcher หลังอัปเดตและเก็บไฟล์ชั่วคราวให้เรียบร้อย

## 0.1.7 - เพิ่ม custom launcher รุ่นแรก

- วาง launcher ของ LumaCue สำหรับตรวจและติดตั้ง update ก่อนเปิด desktop app

## 0.1.6 - เพิ่มหน้า About / Updates

- เพิ่มหน้า About / Updates พร้อมเวอร์ชัน, สถานะ update, ความคืบหน้า และ OBS URL

## 0.1.5 - แก้ข้อมูล shortcut บน Windows

- ปรับ metadata ของ shortcut และการเปิดแอปจาก Windows

## 0.1.4 - เพิ่มหน้าตรวจอัปเดตตอนเปิดโปรแกรม

- เพิ่มหน้าจอตรวจและติดตั้ง update ตอนเปิดโปรแกรม

## 0.1.3 - ติดตั้งอัปเดตเบื้องหลัง

- ติดตั้ง update เบื้องหลังโดยให้ LumaCue แสดงสถานะแทน

## 0.1.2 - แก้การแพ็ก Desktop App และขั้นตอนหลังติดตั้ง

- ปรับการแพ็กโปรแกรมและขั้นตอนหลังติดตั้ง

## 0.1.1 - รุ่นสาธารณะรุ่นแรก

- ปล่อย LumaCue desktop รุ่นแรก พร้อม installer และระบบอัปเดตอัตโนมัติ
